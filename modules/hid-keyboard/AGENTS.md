# HID Keyboard Module Agent Guide

- Keep reusable BLE keyboard GATT, pairing, report, and typing behavior in `HIDKeyboardServer.ts`.
- Keep the public import `moddablue/hid-keyboard/server` stable.
- Keep UI and provider adapters in `examples/hid-keyboard/`.
