# HID Media Control Module Agent Guide

- Keep reusable BLE Consumer Control GATT, pairing, and report behavior in `HIDMediaControlServer.ts`.
- Keep the public import `moddablue/hid-media-control` stable.
- Keep UI and provider adapters in `examples/hid-media-control/`.
